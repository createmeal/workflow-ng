
export interface ActionColumn {
  name: string,
  icon: string,
  color: string,
  onClick: Function | undefined
}
export interface EntityColumn {
  name: string,
  label: string,
  visible: boolean
}